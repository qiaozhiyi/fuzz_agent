## 2024-05-30 - Centralize string utilities\n**Learning:** The `trim` and `json_escape` functions are duplicated across many source files in this codebase, representing a code size, maintainability, and compilation time bottleneck.\n**Action:** Extract these to a centralized `string_util.hpp` and `string_util.cpp` using `std::string_view` to avoid string allocations where possible.
